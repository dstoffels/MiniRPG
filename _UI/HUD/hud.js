export default class HUD {
	constructor(player) {
		this.player = player;
		this.display();
	}

	display() {
		// display menu bar?
		// display stats bar
		//   - need stat meters
		// display anatomy
		//  - stat meter for each bp?
		// how to view skills/attributes?
	}

	update() {}
}
